export default function Home() {
  return (
    <main style={{ padding: 24, fontFamily: "system-ui" }}>
      <h1>Outlook → Google Calendar Timezone Fix</h1>
      <p>
        If you can read this, the app is deployed correctly.
      </p>
      <p>
        Calendar endpoint:
        <br />
        <code>/api/calendar</code>
      </p>
    </main>
  );
}
